@page
@model IndexModel
@{
    ViewData["Title"] = "Página Inicial";
}
<!-- Header-->
<header class="hero-header py-5">
    <div class="container px-5">
        <div class="row gx-5 justify-content-center">
            <div class="col-lg-6">
                <div class="text-center my-5">
                    <h1 class="display-5 fw-bolder text-white mb-2">OdontoDente: Validação Inteligente de Procedimentos</h1>
                    <p class="lead text-white-50 mb-4">Sistema inovador de validação de procedimentos odontológicos através de inteligência artificial para a Odontoprev, garantindo a qualidade e a veracidade dos atendimentos.</p>
                </div>
            </div>
        </div>
    </div>
</header>

<!-- Features section-->
<section class="py-5 border-bottom" id="features">
    <div class="container px-5 my-5">
        <div class="row gx-5">
            <div class="col-lg-4 mb-5 mb-lg-0">
                <div class="feature bg-primary bg-gradient text-white rounded-3 mb-3"><i class="bi bi-camera-fill"></i></div>
                <h2 class="h4 fw-bolder">Validação por Imagem</h2>
                <p>Nossa plataforma utiliza tecnologia de ponta em inteligência artificial para analisar imagens do antes e depois dos procedimentos odontológicos. Isso permite uma verificação precisa e eficiente da execução dos tratamentos, garantindo a qualidade do serviço prestado aos pacientes da Odontoprev.</p>
            </div>
            <div class="col-lg-4 mb-5 mb-lg-0">
                <div class="feature bg-primary bg-gradient text-white rounded-3 mb-3"><i class="bi bi-shield-check"></i></div>
                <h2 class="h4 fw-bolder">Segurança e Confiabilidade</h2>
                <p>O OdontoDente garante a segurança e privacidade das informações dos pacientes e profissionais. Todo o processo de validação é realizado com a máxima confidencialidade, seguindo rigorosos protocolos de proteção de dados e em conformidade com a LGPD, proporcionando tranquilidade para dentistas e pacientes.</p>
            </div>
            <div class="col-lg-4">
                <div class="feature bg-primary bg-gradient text-white rounded-3 mb-3"><i class="bi bi-graph-up"></i></div>
                <h2 class="h4 fw-bolder">Eficiência Operacional</h2>
                <p>Através da automatização do processo de validação, reduzimos significativamente o tempo de aprovação dos procedimentos. Nossa solução permite que os dentistas foquem mais no atendimento aos pacientes, enquanto garantimos a autenticidade e qualidade dos serviços prestados, otimizando todo o fluxo operacional da Odontoprev.</p>
            </div>
        </div>
    </div>
</section>

<!-- Updates Section -->
<section class="py-5 bg-light" id="updates">
    <div class="container px-5 my-5">
        <div class="row mb-5">
            <div class="col-12">
                <h2 class="fw-bolder mb-4">Últimas Atualizações</h2>
            </div>
        </div>
        <div class="row gx-4">
            @foreach (var post in Model.Posts)
            {
                <div class="col-lg-4 mb-5">
                    <a href="/Post/@post.Id" class="text-decoration-none text-dark">
                        <div class="card h-100 shadow-sm hover-card">
                            <img src="~/images/card_bg.jpg" class="card-img-top" alt="@post.Title" style="height: 200px; object-fit: cover;">
                            <div class="card-body">
                                <h5 class="card-title fw-bold">@post.Title</h5>
                                <p class="card-text text-muted">@(post.Content.Length > 150 ? post.Content.Substring(0, 147) + "..." : post.Content)</p>
                            </div>
                            <div class="card-footer bg-transparent border-top-0">
                                <small class="text-muted">
                                    <i class="bi bi-calendar3"></i>
                                    @post.CreatedAt.ToString("dd/MM/yyyy")
                                </small>
                            </div>
                        </div>
                    </a>
                </div>
            }
        </div>
    </div>
</section>